# 临时交换目录

本目录是开发文档的临时交换位置，不是结论、Spec、计划或证据的长期权威来源。新材料使用 `YYMMDD-` 前缀；在任务边界前必须逐份归入唯一主题的 `history/`、提炼为对应主题的 living 文档，或写明理由后删除。

当前没有临时 payload。此 README 受版本控制，只为使空目录能在新的 `dotdev` checkout 或 worktree 中恢复；它不表示目录中的任意未来文件可被批量删除。
