# 本轮重构复查核查清单

- 复查对象：`/home/xp/src/ghc-api-proxy-py` 当前工作树，调查开始时 `HEAD` 为 `061aff32`，另有未提交的跨模块重构；任何发现必须区分既有代码与工作树改动，不能假定某项改动归本轮所有。
- 目标：给出下一步重构切片的建议，并只挑选不与现有未提交变更重叠、可由当前事实直接证实的小错误。
- 范围一：检查配置/依赖注入/模型提供者重构是否保留唯一的事实源、是否出现反向依赖或兼容层滞留。
- 范围二：检查请求生命周期/观测记录/回复路径的事实是否在不同交付模式下发生重复推导或默认值伪造。
- 范围三：检查当前差异中的测试是否仍断言真实场景，并检查架构边界测试是否覆盖新增模块。
- 不在本轮执行：改写产品代码、格式化、运行会修改工作树的生成器、修改人类控制文档、触碰 `4141` 服务。
- 处置原则：调查报告中的事实必须由协调者基于当前树独立复核；若发现需要大范围重构，只作为建议交给用户，不隐式扩展本轮范围。
