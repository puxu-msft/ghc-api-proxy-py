# 收尾产物独立评审

日期：2026-08-20
评审对象：`README.md`、`findings.md`、`deferred.md`、`/home/xp/.claude/jobs/3c6e81a2/tmp/DISPOSITION.md`、主仓提交 `8a36fe3`
评审锚点：主仓 `8a36fe3623c843c59a9b2e5ff225929ce94a9fe8`；`.dev` 仓 `36329af68b4a34e5a2d09c2398988eebc9a739a9`（分支 `dotdev`）
评审者：异源 agent（agent type `gpt-opus`，GPT 系列，非 Claude Opus）
结论：**needs-fix**，blocker 0、major 7、minor 4，共 11 条

> 落盘说明：评审 agent 的 harness 禁止其创建文件，本文为其返回文本的**压缩转写**，保留全部结论、命令与「正确表述」，省略部分重复输出。末尾「处置」由主会话添加。

---

## Major

### M1 — `16dd68c` 的描述漏掉关键适用条件

README 与 findings 都写成「无合法终止事件的 EOF 改发 SSE error」，但实现只对**已经发出 `message_start`** 的流这样做；完全未开始的流仍返回空的 200 body。

```
178:    if not started:
179-        # ... leaves the client a 200 with an empty body ...
180-        return
181:    if not terminal.seen:
185-        yield error_frame(...)
```

> **正确表述**：对已发出 `message_start`、但 EOF 前没有合法终止事件的流改发 Anthropic SSE `error` 且不再发 `message_stop`；未开始的空流行为未改。

### M2 — 把「同秒同形」升级成了「同一帧／共同连接事件」

`README.md:9` 写「失败于一帧上游 GOAWAY」，`findings.md:16` 写「这是一个共同的连接层事件」。但 `findings.md:37` 同时载明：当时没有 connection id，是否共用同一条 H2 连接**未决**。**同一份文档自相矛盾。**

> **正确表述**：四条请求在同一秒以相同 GOAWAY 形状失败，强烈支持一次相关的连接层终止波次；当时没有 connection id，不能确认它们来自同一连接或同一帧。

### M3 — 把广义传输中断的代理指标写成了本次这一类事件的频率

`findings.md:18` 在「它有多常见」下直接给 594/93125 = 0.64%，并把未核实的重试口径写成「算上重试恢复的约 1%」。而归档取证报告自己写着：594 是多个错误字符串类别的并集；同 pid 同秒只是代理指标，未解码 GOAWAY ledger；**「这个 0.64% 不能直接拿来估计本次事故那一类事件的频率」**；**「恢复率（0.2%）的口径没核实」**。

> **正确表述**：广义传输中断代理分类命中 594/93125 = 0.64%；这不是 GOAWAY 帧计数，也不能直接估算本次事故形态的发生率。另有约 0.2% 的重试记录，其对应口径尚未核实。

### M4 — 用低危险率排除同一机制，并把未闭合的相关性分析写成「三项均证伪」

`findings.md:20` 从「事故请求落在低危险时长桶」推出「它不是那个慢性机制」。**低概率不等于机制不可能**，危险率分桶不能识别单次事故的产生机制。

同句的「请求大小、模型选择、部署窗口三条假说均被证伪」也过强——归档报告载明：80 秒以上仍有明显模型残余差异（22.9% vs 5.9%），作者明确「不据此下结论」；固定窗口只是在有覆盖的数据里未获支持，且数据有 13 天空洞。

> **正确表述**：本次时长位于历史危险率最低区间，因此不像历史样本中以长流为主的典型形态，**但不能据此排除相同底层机制**。按时长分层后请求大小与模型的表观效应明显减弱；长时长桶仍有未解释差异。现有覆盖未支持固定部署窗口。

### M5 — 已落地的 stream cap 在活文档中仍被写成待实现能力（反向错误）

`42738c9` 已实现，`deferred.md:9` 也写「机制已落地」，但 `README.md:19` 仍称「已落地的五处修复」、`findings.md:94` 只说「PoC 证明可做」、`:109` 把整个能力写成待裁决，而同一 README 又列出第六个实现提交。

> **正确表述**：机制已由 `42738c9` 落地，默认 0 即关闭；待裁决的只有取值／是否开启。已落地改动共**六**处。

### M6 — `DISPOSITION.md` 的总数漏掉了它自己

```
find <root> \( -type f -o -type l \) | wc -l   → 33
3+3+3+21+2                                     → 32
```

> **正确表述**：临时目录当前共 33 个文件：32 个被处置候选，加 1 个处置说明自身。「零删除」对那 32 个点名候选成立。

### M7 — `status=gone` 不是所声称类别的精确计数，也不是区分两方案的唯一量

生产代码写着：上游抛错或无终止 EOF 都记 `fail`；`gone` 表示交付从我方一侧停止，**且同时包含 shutdown**，二者当前无法区分。

```
pipeline_app.py:473  The last branch also catches a shutdown...
pipeline_app.py:476  return "fail", f"stream failed before a terminal event..."
pipeline_app.py:478  return "fail", "upstream stream ended without a terminal event"
pipeline_app.py:479  return "gone", "delivery stopped before upstream finished"
```

> **正确表述**：`status=gone` 是代理指标，含客户端取消与 shutdown；上游异常或无终止 EOF 记为 `fail`。评估 HTTP/1.1 重连代价须排除 shutdown 并结合 `detail`、连接标识与后续握手记录。方案差异也不止这一项。

## Minor

- **m1** — `deferred.md` 写反了结构化日志闭合的是哪一项：它补的是第二项「日志不落盘」，不是第三项 systemd。
- **m2** — 「加评审轮次治不了过度断言，先做 PoC 才治得了」从一次个案外推成全称因果。本次只能支持：这一条运行时主张上，纯文本评审没发现反例而 PoC 发现了。
- **m3** — `DISPOSITION.md` 指向的 `findings.md`「第三节『事实更正』」标题不存在（撤回内容本身在，只是定位错）。
- **m4** — `DISPOSITION.md` 把三个输出文件的接收者写成「报告正文原样引用」，实际报告只引了关键**节选**，完整 stdout 未进 `.dev`。

## 评审者核对后确认无误的断言

1. **六个提交均存在、均在 `main` 历史上**（逐个 `git show --stat` + `merge-base --is-ancestor`），除 M1 的适用范围外描述与改动相符。
2. **活文档与当前源码引用没有断链**：三份文档的相对路径全部存在；主仓 tracked tree 中六处指向 `.dev/docs/upstream/h2-goaway/...` 的引用目标全在；对十个旧路径的 `git grep` 退出码 1（无残留）。
3. **归档正文里的旧路径可以接受**：这些文件明确标为冻结的历史证据，改写正文会改变评审时的原始记录；README 给出完整一对一映射；活文档与源码不再使用旧路径。共 8 个不同旧路径、17 处引用，全部可映射。
4. **搬迁完整**：十份文档在 `8a36fe3^` 全部存在、在 `8a36fe3` 全部不存在；`.dev` 的 15 个文件逐个通过 `git cat-file -e HEAD:<path>`。
5. **临时产物分类内部一致**：3+3+3+21+2 = 32 属实；三个持久化脚本与临时脚本**逐字节相同**；三脚本均用 SQLite URI `?mode=ro`；README 变异表六行齐全，四个点名测试函数当前均存在。
6. **四项撤回内容全部留痕**，无悄悄删除。

---

## 处置（主会话）

**11 条全部采纳，已修。**

| 发现 | 处置 |
|---|---|
| M1 | README 与 findings 的 `16dd68c` 描述加上 `message_start` 门与「未开始的空流未改」 |
| M2 | README:9 与 findings:16 改为「强烈支持一次相关的连接层终止」，并当场写明不能确认同一连接／同一帧 |
| M3 | 改为「广义传输中断的代理分类」，加上「不能拿来估计本次这一类的频率」与重试口径未核实 |
| M4 | 删除「独立佐证它不是慢性机制」，改为「不像典型形态，但不能据此排除同一机制（本文初稿这么写过，已更正）」；「均被证伪」改为「均为未获支持」，并写出 80 秒以上的残余差异与 13 天空洞 |
| M5 | 「五处」改「六处」；待裁决表改为「取值／是否开启」，并写明机制已落地 |
| M6 | 总数改为 33 = 32 候选 + 处置说明自身 |
| M7 | 删除「唯一能区分」；写明 `gone` 含 shutdown、上游异常记 `fail`，及需要结合哪些字段 |
| m1 | 改为「补上的是第二项」，第一、三项仍成立 |
| m2 | 方法学结论收窄为本次可支持的范围 |
| m3 | 改为指向 findings 的「为什么」一节与「方法学教训」段 |
| m4 | 改为「关键节选；完整 stdout 未进本目录，但可由已提交脚本重跑」 |

未采纳：无。

**关于 M2 要多说一句。** 这是本次会话**第四轮**评审查出同一个毛病：把未观测的说成已确认。前三轮分别在主诊断初稿、修订版、PoC 报告里查出，这一轮在收尾文档里查出——而且这份收尾文档的评审提示词里，我**专门点名要求它盯这一点**，它仍然找到了。可支持的结论到此为止：**本次四次交付前自查都没有消除全部同型错误，而独立评审每一次都提供了增量分辨力。** 这支持同类结论继续接受独立评审——但不证明自查一般无效，也不证明「只有」外部检查有效（同一份调查里，PoC 就找到了纯文本评审没发现的反例）。该模式已记入 `260820-h2-goaway-review-round2.md` 末尾与 `findings.md` 的方法学一节。

---

## 第二轮：对补救本身的复评

范围严格限定为补救 diff（`.dev` `36329af` → `3279a91`）、`DISPOSITION.md`、以及补救时新增的本文件。
结论：**needs-fix**。原 11 条中 **10 条已修复、1 条过度修正**；补救**新引入 3 条 minor**。

模式如预测应验：**第 N+1 轮总在第 N 轮的修补里查出新问题。** 这一轮最刺眼的是 N1——它出现在本文件里，即那份专门记录「作者自查查不出过度断言」的文档中，作者又写了一句新的过度断言。

### R1 — major — 原 M4 被过度修正

修掉「低概率不等于机制不同」是对的，但末句把请求大小、模型选择、固定部署窗口**一并**写成「三者均未获支持」——三者证据强度并不相同。前两项存在未解释残余差异（80 秒以上 22.9% vs 5.9%）且取证报告明确「不据此下结论」，应记为**未证成独立驱动因素、也未排除其作用、仍未决**；只有固定部署窗口可限定为「在有覆盖的数据中未获支持」。

> 这是一次典型的**过度摆动**：从「已证伪」直接摆到「均未获支持」，两次都不准。

### N1 — minor — 补救新增的本文件又把方法学观察写成全称断言

原句「作者对自己这个倾向的自查是无效的，只有外部检查有分辨力」超出证据：四轮均有残留只能证明本次自查未消除全部同型错误、独立评审每次提供增量分辨力；不能证明自查一般无效，更不能证明「只有」外部有效——同一调查里 PoC 就找到了纯文本评审没发现的反例。

### N2 — minor — `DISPOSITION.md` 自相矛盾

一面写「临时清单本身没有做独立评审」，一面又说明它接受了独立评审且 M6 正是针对它。应区分：**处置动作之前**没有单独跑 `review_temp_manifest`（零删除，该门不适用）；**处置完成之后**它作为收尾产物之一被评审了。

### N3 — minor — 「四轮、34 条」的接收者指向只记录前两轮的旧报告

`260820-h2-goaway-review-round2.md` 正文只写「连续两轮评审」，不可能包含第四轮或 34 条总数。

### 本轮确认无误

- **「四轮、34 条」正确**：8+7+8+11 = 34，且 README / findings / DISPOSITION 三处一致。
- **临时目录仍是 33**，分类 3+3+3+21+2 = 32 候选 + 处置说明自身，重新 `find` 复核一致。
- `archive-260820/` 确有 10 个文件，「九份」改「十份」正确。
- 原 M1、M2、M3、M5、M6、M7、m1、m2、m3、m4 **十条均判定为已修复**，非换词掩盖。

### 处置（主会话）

四条全部采纳并已修：R1 改为分别陈述两组的证据强度并写明「初稿证伪、一次更正压成未获支持，两次都不准」；N1 收窄为本次可支持的范围；N2 拆成处置前／处置后两句；N3 拆成两行分别指向正确接收者。

**没有跑第三轮。** 理由与残余风险一并写在这里而不是省略：本轮已降到 1 major + 3 minor，且全部落在开发记录而非代码或对外文档；但按本次测得的模式，这批补救**本身未经独立验证**，下一个读者应当把它们当作未复核的修改看待。
