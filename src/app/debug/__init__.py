"""What the `debug` CLI subcommands report.

These read state; they never change it. Each one answers a question an operator asks while something is wrong, so they are built to run against the same wiring the server uses rather than against a server that may not be up.
"""
