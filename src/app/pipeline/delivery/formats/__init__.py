"""Everything that knows a wire format, named for the format it knows.

A format's assembler and its framer live in one module. Anything else specific to one format carries its name as a prefix — the file list says which formats exist and what each of them does.
"""
