# 收尾遗漏评审（2026-08-22）

评审对象：会话 `bbb2909c-7abe-4d22-85bc-d33af0b9eec7`（`2026-08-21T17:40:18Z` → `2026-08-22T16:43Z`）的收尾记录完整性。
唯一职责：找出**在本会话中被证实、但没有被写进任何耐久承载物**的知识。

事实源：transcript `~/.claude/projects/-home-xp-src-ghc-api-proxy-py/bbb2909c-7abe-4d22-85bc-d33af0b9eec7.jsonl`（1750 行）。方法按派发要求执行：**先从 transcript 独立枚举 42 条候选，再读作者的承载物做双向对账**。

对账的承载物范围：
- `.dev/docs/tmp/260822-cli-and-module-move-closeout.md`
- `.dev/docs/tmp/260821-shared-index-left-reverting-head.md`
- 项目记忆 `git-commit-takes-the-whole-index.md`（及其引用的其它记忆条目）
- 主仓库 7 个提交的提交信息（`ff0ac3c` `5a1bb94` `b9939ca` `d49fe23` `5fc9dc4` `92725a4` `5cb8dcf`）

## 先说结论

**作者这次记得相当完整。** 收尾文档的「三处命令跑了、输出真实、结论仍然错」「执行过的变异及其失败形状」「一条被放弃的路线」三节，覆盖了我独立枚举出的类别 2、3、5 的绝大部分，而且**变异那节把「红得不对」（子进程崩溃而非断言触发）如实记了下来**——这是同类记录里最容易被抹平的一处。提交信息也不是索引，是真的在承载判据（`5a1bb94` 把 `GHC_` 前缀冲突的机制完整写进去了）。

所以本轮 5 条 major **没有一条是「作者忘了记那个坑」**，全部是另一种形状：**作者记下了症状，没记下机制；或者记下了当时的应急做法，没记下同一会话稍后找到的正解。**

先例说「一轮不够」。本轮验证了这一点，但方式不同：第一次用这个流程时漏的是显眼的坑；这次漏的是**坑的可推广形式**。

---

## 一、遗漏（我找到、作者没列）

### M1 [major] `ruff check --stdin-filename` 是「副本目录里 ruff 误分类」的正解，作者用了却没记

**作者记了什么**：收尾文档 §「一条被放弃的路线：不要在副本目录里用 ruff 重建 import 顺序」。结论停在——

> 改用按明确锚点手工搬那一个 import 块，并用一句断言校验 `app.*` 的字母序。

**transcript 里发生了什么**：14 分钟后，作者在同一个问题上找到了机械化的解法。

- `2026-08-21T18:15:15.888Z`：`scratch 环境里 ruff 误判了 httpcore2 的分类，重排结果不可信。改为按明确锚点手工搬那一个 import 块，不依赖 ruff。`
- `2026-08-21T18:29:30.895Z`（记录 `[1201]`），在**主树 cwd** 下校验一份只存在于临时目录的重建内容：

```bash
cd /home/xp/src/ghc-api-proxy-py
S="$CLAUDE_JOB_DIR/tmp/recon-cli"
uv run ruff check --stdin-filename src/app/cli.py - < "$S/cli.py" 2>&1 | tail -2
```

输出：`All checks passed!`（记录 `[1203]`）。

**为什么这是遗漏而不是小事**：`--stdin-filename <仓库内路径>` 让 ruff 按**仓库根**解析配置与第一方包分类，同时内容来自任意位置的临时文件——它精确地消灭了 §「一条被放弃的路线」记录的那个失效（副本目录里 `httpcore2` / `httpx2` 被判成非第三方）。而文档现在把「手工搬 import 块 + 一句字母序断言」写成了唯一出路：那是不可复用的，下一次文件更大、import 更多、或者要动的不止一个 import 块时就不成立。

**承载物**：无。收尾文档、提交信息、项目记忆里均无 `--stdin-filename`。

**建议**：在 §「一条被放弃的路线」末尾补一行——校验（而非重排）重建内容用 `ruff check --stdin-filename <仓库内路径> -`，在仓库根 cwd 下跑，配置与包分类都对；只有**重排**才必须手工。

---

### M2 [major] 「从 HEAD 重建目标内容，再与工作树逐字节比对」是本会话真正的主力判据，用了三次，一次都没写下来

**transcript 证据**（三次，每次都决定了下一步动作）：

1. `2026-08-21T18:15:37.063Z`——`重建结果与工作树版**逐字节相同**——说明 composition.py 的工作树内容相对 HEAD 只有我那一处改名，同伴的差异在索引侧。`
2. `2026-08-21T18:21:16.234Z`——同一手法抓到真问题：`!!! 携带非改名内容的文件: - tests/int/test_pipeline_app.py`。这一次比对**不相同**，于是该文件走了单独重建 + `--cacheinfo` 的路径（记录 `[997]` 第 4 步）。
3. `2026-08-21T18:29:22.598Z`——`重建版与工作树版：差异应只有同伴那处文档路径`，输出 `288c288 < # in \`docs/agents/...\``，据此把同伴那一行排除出提交。

**承载物**：无。而且这里有一处**承载物之间的错位**——项目记忆 `git-commit-takes-the-whole-index` 现有的归属判据是：

> 判据很简单：`git diff -- <path>` 里有没有不是你写的 hunk。

那是**人工逐 hunk 认领**。同一份记忆的 §「别用『按内容标记筛 hunk』代替配方里的内容级构造」（2026-08-22 写入）正好记载了这条判据怎么失效：共享 hunk 里同时有两个人的名字、用自己列的黑名单证明「没混入别人的东西」只能证明没混入自己列出的那几样。

**「从 `git show "$OLD:$P"` 重建完整目标内容，再与工作树逐字节比对」不依赖任何归属推断**——差异为零就机械地证明了工作树里只有你的改动，差异非零就逐行读那几行。它是那条已知失效的正解，本会话实测有效三次（含一次真的抓到夹带），而它现在只活在 transcript 里。

**建议**：写进项目记忆 `git-commit-takes-the-whole-index`，紧挨着「别用按内容标记筛 hunk」那一节——因为配方本来就要求内容级构造，这条只是给它补上**验收步骤**：构造完先与工作树 `diff`，差异必须能被你逐行认领。

---

### M3 [major] 从 transcript 收割提交清单的两处漏配，作者当场发现并口头说了，收尾文档只留了结论

**transcript 证据**：

- `2026-08-22T16:44:04.344Z`——`回执模式**漏掉了 5fc9dc4**（那次我没打印 new commit:）——正好印证了收割是超集也可能是子集。`
- `2026-08-22T16:44:28.164Z`——`注意回执模式两次都是子集（git -C ... commit --only 也没被匹配到），以 git log 为准。`

第二处的机制在记录 `[1641]` 的命令里可读：

```bash
rg -o 'git (commit-tree|commit --only|commit -|update-ref|mv|reset|update-index --force-remove|rm --cached|add --)'
```

对 `git -C /home/xp/src/ghc-api-proxy-py commit --only …`（记录 `[645]`，即 `5a1bb94`）**不匹配**，因为 `-C <path>` 插在 `git` 与子命令之间。计数输出 `3 git commit --only`，而实际有 4 次。

**为什么这条会系统性重现**：`git -C <绝对路径>` 是本项目的**既定习惯**——用户规则 `root-each-bash-call` 要求每次 Bash 调用显式绑定目录，`long-flags-over-short` 之外的这条正是推荐写法。所以任何「扫 transcript 里的 git 命令」的收尾流程，在这个项目上都会稳定漏掉一部分。

**承载物**：无。收尾文档只写了结论「主仓库本会话共 7 个提交」，方法论一个字没有。项目记忆 `the-pattern-is-not-the-predicate`（模式不等于判据）在讲同一族问题，但没有这个具体形状。

**这是最典型的「作者最看不见自己刚做过的事」**：`16:44:28` 说出口，`16:47:24` 写收尾文档，中间隔了 3 分钟。

**建议**：一句话进收尾文档——从 transcript 收割提交清单只能当**候选集**，两个方向都会失真（自定义回执可能没打印；`git -C <path>` 会让子命令模式漏配）。权威源是 `git log --since=<会话起点>` 加人工归属。

---

### M4 [major] 私有索引配方缺「批量搬迁（rename）」形态，而本会话四次提交跑的都是这个形态

**transcript 证据**：

- `2026-08-21T18:15:49.484Z`——`GIT_INDEX_FILE=<私有索引> git add -A -- <15 条显式路径>` 被护栏拦下：`⛔ git 纪律护栏拦截：git add -A / --all / .`。**护栏只按命令形态拦截，不知道你指向的是私有索引**，所以私有索引里也用不了 `-A`。
- `2026-08-21T18:16:00.031Z`——`改成显式列出新增文件 + 从私有索引中显式移除旧路径。`
- 实际形态（记录 `[997]`，43 条路径含 17 条重命名）：

```bash
export GIT_INDEX_FILE="$IDX"
git read-tree HEAD
OLD=$(git ls-tree -r --name-only HEAD -- src/app/ghc_client tests/unit/ghc_client)
echo "$OLD" | while read -r f; do [ -n "$f" ] && git update-index --force-remove -- "$f"; done
NEW=$(git ls-files --others --cached --exclude-standard -- src/app/model_provider/ghc_client … | grep -v __pycache__ | sort -u)
git add -- $NEW
```

**承载物**：无。项目记忆里的配方明确写着「本配方一次只提交一条路径」，并有一道 `if [ "$MODE" = 040000 ]` 的门把目录判停。记忆的 §「2026-08-22 第三次击发」记了 `--add` 变体，**没有记 rename 变体**，也没有记「护栏会拦 `git add -A` 即使目标是私有索引」。

**这一点不是可有可无的**：本会话的索引事故（漏做末尾对齐）现场，用的正是这个配方之外的写法。记忆的 §「配方末尾那条索引对齐不是可选装饰」已经把根因写透了（`update-ref` 不动索引），但**配方本身仍然只覆盖单路径**，下一个做模块搬迁的人还是得现场发明这套 `--force-remove` + `git add --` 的组合。

**建议**：把上面这段 rename 形态补进记忆的配方，并注明护栏会按形态拦 `git add -A`（私有索引也不例外），以及末尾对齐对多路径要写成循环或直接 `git reset -- <路径…>`（记忆 §第三次击发已给出后者）。

---

### M5 [major] 残留检查复用了施工时的那个 pathspec，结构上无法证伪它要查的那个范围错误

这条是本轮我认为最有价值的一条，因为**作者记下了症状，没记下机制**。

**作者记了什么**：收尾文档 §「改名的搜索范围：只扫 `src/` 和 `tests/` 会漏掉已跟踪的 `exp/`」，判据是「改名的搜索范围是整个仓库」。

**transcript 里真正发生的**（记录 `[894]`，`2026-08-21T18:17:20.783Z`）——同一条命令里，施工与验收用了**同一个 pathspec**：

```bash
FILES=$(rg -l "app\.ghc_client" --glob '!__pycache__' src/ tests/)     # ← 施工范围
…（对 $FILES 做替换）…
echo "--- 残留检查（应为空）---"
rg -n "\bapp\.ghc_client\b" --glob '!__pycache__' src/ tests/ || echo "(无残留)"   # ← 验收范围，同一个
```

输出：`总替换处数: 73` / `(无残留)`。**这句「无残留」是真的**——在 `src/` 与 `tests/` 里确实一处不剩。而 `src/` 与 `tests/` 恰好是刚刚被整体重写过的那个域。

`2026-08-21T18:22:52.574Z`（记录 `[1039]`）的提交态复查有同样的缺陷：`git grep -n "\bapp\.ghc_client\b" HEAD -- src tests`。

**后果的量级**：这个漏洞让 `exp/` 下 5 个已跟踪文件的断裂 import 在 `main` 上活了将近 23 小时（`18:17` → 次日 `16:45`），期间跨越四次全量 sweep、一次「任务和讨论都结束了」的收尾、以及一次用户主动要求的索引与工作树体检。**没有任何一次给出过失败信号。**

**可推广的机制**（这才是该写下来的东西）：

> **验收的范围必须严格大于施工的范围。** 用施工时的那个 pathspec 去做残留检查，检查结论恒为「无残留」，并且没有失败信号——因为你查的正是你刚改完的那个集合。正样本对照也救不了：正样本同样落在那个域里。

顺带一提，这条命令的 `|| echo "(无残留)"` 写法**是对的**（`rg` 无匹配退出 1，`||` 只在真无匹配时触发），不属于项目记忆 `never-echo-the-conclusion-beside-the-command` 那一类。作者在 `18:22:34` 还专门自查过一次固定文本的问题，并在收尾扫描（记录 `[1669]`）里写了「不打印任何结论文字」——**那条纪律已经内化了**。缺的是这条独立的、关于范围的。

**建议**：这条不该只写进本会话的收尾文档，它是一条可复用的验收纪律，值得进项目记忆（或补进 `the-pattern-is-not-the-predicate`——那条已经在讲「模式排除了该看的行」，这条是它的一个特化：**排除该看的行的那个 pathspec，正是你施工时用的那个**）。

---

### m6 [minor] `git archive HEAD | tar -x` 出来的副本树里，含有指向仓库外真实目录的符号链接

**证据**：

- `2026-08-22T16:48:55.720Z`——`/home/xp/.claude/jobs/bbb2909c/tmp/head-checkout-2/refs/CLIProxyAPIPlus -> /home/xp/src/refs/CLIProxyAPIPlus`（两棵检出树各一条）。
- 我复核了它的来源：`git ls-tree HEAD refs/` 给出 `120000 blob 5ffbbce6… refs/CLIProxyAPIPlus`——这是仓库**已跟踪的符号链接**，`git archive` 会忠实检出。

**为什么值得记**：索引事故文档 §「一个后续教训」已经把 `D=$(mktemp -d); git archive HEAD | tar -x -C "$D"` 写成了可复用配方（而且是唯一能发现「HEAD 变成半截」的手段），但没提这一点。副本树因此**不是一个封闭沙箱**——它有一条通向 `/home/xp/src/refs/` 真实目录的路。

**范围声明（不夸大）**：`rm -rf <副本树>` 本身**是安全的**，`rm` 不跟随符号链接，删掉的是链接本身。危害只在使用**解引用**的工具时出现：`find -L`、`cp -rL`、不带 `--no-links` 的 `rsync`、`du -L`，以及任何按 `refs/…` 相对路径向副本树内写文件的脚本。本会话采用了零删除处置，没有踩到。

**建议**：在索引事故文档那个代码块下面加一句范围提示即可。

---

### m7 [minor] 「干净检出跑出红灯」的第一嫌疑应该是副本缺件，这个替代解释是怎么被排除的没写下来

**证据**：

- `2026-08-22T07:48:41.774Z`——`先别下结论——可能是 HEAD 真坏了，也可能是副本树缺了未跟踪文件。抓子进程的实际输出`
- 排除方式（记录 `[1490]`）是**抓到了具体的 `TypeError`**，而不是证明副本完整。

**承载物**：部分。索引事故文档写了「先证明探针命中副本」——那回答的是「跑的是哪棵树」，不是「这棵树完不完整」。项目记忆 `prove-the-probe-ran-before-reading-its-number` 里有「副本树缺 `pyproject.toml`」一条，形状相近但不是同一件事。

`git archive` 只导出**已跟踪**文件，所以副本树天生缺未跟踪文件（本地 `.env`、未提交的 fixture、缓存）。**副本树上的红灯，第一嫌疑永远是缺件，不是 HEAD 坏。** 排除它的正确做法就是本会话做的那个——拿到具体报错，确认它指向的是代码而不是缺失的文件。

**建议**：索引事故文档那个代码块的第二行注释旁边补一句：红灯先看报错内容，别直接归因 HEAD。

---

### m8 [minor] `.dev` 侧的提交没有出现在任何交付清单里

**证据**：本会话在 `.dev` 仓库提交了 `ccb1eba`（`21:12:42`）、`a96b324`（`07:54:16`）、`59543b1`（`16:47:34`）。收尾文档的交付表只有主仓库 7 条；正文中的「`.dev` 2 个」（`16:44:28` 口头）在文档写完时就已经是 3 个了。

**承载物**：部分——文档本身在 `.dev/docs/tmp/` 里，`git -C .dev log` 可查。属索引不全，不是知识丢失。

**补充观察**（供主会话判断，不是发现）：我复核时 `.dev` 已有 `1197da7`（16:50）与 `e427985`（16:52），都在本次评审派发之后。收尾文档写下的「未派遣 subagent」在写下那一刻为真，两分钟后失效。这属于收尾文档的时效性，不是遗漏。

---

### m9 [minor] 本会话 7 次 `update-ref` 的 CAS 拒绝分支一次都没被触发

**证据**：四次业务 CAS（`18:16:26`、`18:22:39`、`18:23:20`、`18:29:41`）全部成功落地。`18:22:39` 那次还在 CAS 之前自己加了一道 `if [ "$NOW" != "$BEFORE" ]; then … exit 1; fi`，等于把冲突挡在了 CAS 之前。

`2026-08-21T18:16:38.294Z` 的这句需要正确理解——

> 期间同伴往 `main` 提交了 `ae472f3`，CAS 正确地接在了它后面。

那是 `BEFORE=$(git rev-parse HEAD)` 在同伴提交**之后**才读的，**不是 CAS 拒绝**。所以本会话没有任何一次实测证明 CAS 会拒绝。

**承载物**：有。项目记忆 §「CAS 不可省」已经写明拒绝行为与报错文本，来自更早的会话。所以这不是新知识丢失，只是**本会话这七盏绿灯在 CAS 这一维度上没有分辨力**——不构成需要补记的内容。

列在这里是为了**防止把它读成「CAS 已在本会话验证过」**。

---

### m10 [minor] 收尾文档的可复用探针代码块，删掉了实际探针里的自证输出行

**证据**：实际探针（记录 `[470]` 与 `[1490]`）都带了一行自证：

- `print("TOKEN VARS:", {k: v for k, v in env.items() if "TOKEN" in k})`
- `print("PYTHONPATH =", env.get("PYTHONPATH"))`

收尾文档 §「可复用」的代码块两行都没有。文档也没写这个探针的**调用方式**：它必须从副本树内、用 `.venv/bin/python` 直接跑（`cd "$D" && PYTHONPATH="$D/src" /home/xp/src/ghc-api-proxy-py/.venv/bin/python probe.py`），因为 `sys.path.insert(0, "tests/systemd")` 是 cwd 相对的，而副本树里没有 `pyproject.toml`、`uv run` 起不来。

**级别理由**：代码块本身是对的，缺的是「怎么证明它跑在你以为的环境里」——而这正是文档同一节强调的那件事（「复用测试模块自己的 `_service_environment`，手搓会漏掉 `Environment=` 条目」）。

---

## 二、反方向：作者列了、我在 transcript 里找不到足额支撑的

这个方向属于并行的事实核查评审的地界，我只报两条与「记录归属」直接相关的。

### A1 [minor] 「手搓环境会漏掉 unit 文件里的 `Environment=` 条目，于是复现出来的是另一个场景」是未验证的反事实

收尾文档 §「可复用」的这句是判断，不是观测。本会话**从未手搓过环境**——两次探针（`[470]`、`[1490]`）都直接 `env = m._service_environment(state, url)`。作者选择了复用，没有实测不复用会怎样。

按 `state-decisiveness`，这句该带上权重档：「这是设计判断，未实测」。

### A2 [minor] 索引事故文档的 §「补记（2026-08-22）」不是本会话产物，派发提示词把它整体计入了「作者写下的承载物」

我复核了归属：

```
86f4f66 2026-08-22 09:55:39 +0000  docs: record that the shared index was stale at more than one point, and the audit that found no second victim
```

本会话对 `260821-shared-index-left-reverting-head.md` 的最后一次编辑是 `2026-08-22T07:54:11Z`（提交 `a96b324`）。补记那一节、以及它引用的 `260822-audit-other-stale-blobs-committed.md`，都出自另一个会话。

**这不是错误**，但会影响对「本会话记录密度」的估计：把补记算进来会高估。我在本报告的对账里已把它剔除——剔除后作者的记录密度仍然很高，M1–M5 的结论不受影响。

---

## 三、完整候选枚举与对账表

「承载物」列只记**耐久**载体：代码注释/docstring、提交信息、测试名、`.dev` 文档、项目记忆。transcript 本身与对话回复不算。

### 类别 1：被放弃的路线

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 1 | 副本目录里用 `ruff --fix` 重建 import 顺序（`httpcore2` 误分类） | ✅ | 收尾文档 §一条被放弃的路线 | — |
| 2 | `rm -rf "$S"` 建 scratch 目录被护栏拦，改用新目录名零删除 | ❌ | 无 | nit |
| 3 | 私有索引里 `git add -A`（护栏按形态拦，私有索引不例外） | ❌ | 无 | **M4** |
| 4 | 用 `new commit:` 回执从 transcript 收割提交清单 | ❌ | 无 | **M3** |
| 5 | `awk '$1 ~ /^[A-Z]/'` 的暂存归属判据 | ✅ | 收尾文档 §三处 第2条 | — |

### 类别 2：被证伪的因果解释

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 6 | 「HEAD 里查不到新变量 → 我的工作丢了」 | ✅ | 收尾文档 §三处 第1条 | — |
| 7 | 「同伴暂存了一个奇怪的旧版本」 | ✅ | 索引文档 §两处归因失误；记忆 §配方末尾 | — |
| 8 | 「`1b0cdd2` 的回退是我的索引修复造成的」（blob 判据证否） | ✅ | 索引文档 §一个后续教训 | — |
| 9 | 「systemd 超时是测试噪音」→ 实为 `GHC_` 前缀冲突启动即死 | ✅ | 提交信息 `5a1bb94`；收尾文档 §决定 | — |
| 10 | 「干净检出红灯可能是副本树缺未跟踪文件」 | ❌ | 部分（记忆 `prove-the-probe-ran…`） | **m7** |
| 11 | 「`composition.py` 的暂存是同伴独有快照」（深度 25→40） | ✅ | 收尾文档 §三处 第3条；索引文档；记忆 | — |

### 类别 3：解析/范围错误（命令跑了、输出真实、结论仍错）

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 12 | f-string 拼出的常量 `git grep` 搜不到 | ✅ | 收尾文档 §三处 第1条 | — |
| 13 | `awk` 吞掉 `git status --porcelain` 前导空格 | ✅ | 收尾文档 §三处 第2条 | — |
| 14 | 「在历史里找不到」是搜索深度的函数 | ✅ | 收尾文档 §三处 第3条；记忆 | — |
| 15 | 改名只扫 `src/` `tests/`，漏掉已跟踪的 `exp/` | ✅ | 收尾文档 §改名的搜索范围；提交信息 `5cb8dcf` | — |
| 16 | **残留检查复用施工 pathspec，结构上无法证伪范围错误** | ❌ | 无 | **M5** |
| 17 | `git -C <path>` 让 `rg 'git commit --only'` 漏配 | ❌ | 无 | **M3** |
| 18 | 残留扫描命中 docstring 里有意保留的历史提及，需人工分流 | ❌ | 有（记忆 `the-pattern-is-not-the-predicate`） | nit |
| 19 | `echo "以上为空"` 固定文本（`18:22:34` 当场自查并纠正） | ❌ | 有（记忆 `never-echo-the-conclusion…`），且已内化 | nit |

### 类别 4：本会话产生或修订的标定值

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 20 | 干净检出 `8469cfa`：1660 passed / 3 skipped / 0 failed | ✅ | 索引文档 §备份 ref 已删除 | — |
| 21 | 干净检出 `9c7e971`：1658 passed / 1 failed（唯一红的是 subprocess 测试） | ✅ | 索引文档 §一个后续教训 | — |
| 22 | 索引修复后工作树全量 1643 passed | ✅ | 索引文档 §修复方式 | — |
| 23 | 工作树历次全量 1211 / 1614 / 1617 / 1639 | ❌ | 无 | nit |
| 24 | 索引与 HEAD 差 51 条 = 我 43 + 同伴 8 | ✅ | 索引文档 §影响面 | — |
| 25 | 窗口内主仓库 84 提交，本会话 7 | ✅ | 收尾文档开头 | — |
| 26 | 备份 ref `refs/backup/index-snapshot-260821` = `91f67a1`（已删） | ✅ | 索引文档 §备份 ref 已删除 | — |
| 27 | 改名波及 43 文件 / 73 处；收尾正样本 75 / 9 / 14 | ❌ | 无（过程量） | nit |
| 28 | `.dev` 侧 3 个提交 `ccb1eba` `a96b324` `59543b1` | ❌ | 部分（`git -C .dev log` 可查） | **m8** |

### 类别 5：实际执行过的变异与正样本对照

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 29 | 变异 `cli.py` 删确认块 → 只打红 `…keeps_an_existing_file_when_the_answer_is_no`，另两条本就不变 | ✅ | 收尾文档 §执行过的变异 | — |
| 30 | 变异 `pipeline/exceptions.py` → 守卫变红，**但形式是 `CalledProcessError`（循环导入崩溃）而非断言触发** | ✅ | 收尾文档 §执行过的变异；提交信息 `d49fe23` | — |
| 31 | 补充证据：新前缀实测命中 10 个真实模块，非空转断言 | ✅ | 收尾文档 §执行过的变异 | — |
| 32 | 收尾扫描先跑正样本（75/9/14 非零）再跑陈旧拼写 | ❌ | 有（记忆 `prove-the-probe-ran…`） | nit |

### 类别 6：运行时/外部能力探针

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 33 | `typer.confirm` 真进程四情形（含无 stdin 时中止不挂住，`< /dev/null` 返回 1） | ✅ | 收尾文档 §决定；提交信息 `92725a4` | — |
| 34 | `git update-ref` 只移动 ref，完全不碰索引 | ✅ | 索引文档全篇；记忆 §配方末尾 | — |
| 35 | `git reset HEAD -- <pathspec>` 只动索引、绝不碰工作树 | ✅ | 索引文档 §修复方式；记忆 | — |
| 36 | 副本目录里 ruff 的第一方/第三方分类会错 | ✅ | 收尾文档 §一条被放弃的路线 | — |
| 37 | **`ruff check --stdin-filename <仓库内路径> -` 是 #36 的正解** | ❌ | 无 | **M1** |
| 38 | 副本树跑测试要用 `.venv/bin/python` 而非 `uv run`（副本缺 `pyproject.toml`） | ❌ | 有（记忆 `prove-the-probe-ran…`） | nit |
| 39 | 副本树含指向 `/home/xp/src/refs/` 的已跟踪符号链接 | ❌ | 无 | **m6** |

### 类别 7：判据与方法

| # | 候选 | 作者已列 | 承载物 | 级别 |
|---|---|---|---|---|
| 40 | **重建 HEAD 版目标内容 → 与工作树逐字节比对**（用了 3 次） | ❌ | 无 | **M2** |
| 41 | 7 次 `update-ref` 的 CAS 拒绝分支一次未触发 | ❌ | 有（记忆 §CAS 不可省，来自更早会话） | **m9** |
| 42 | 收尾文档探针代码块删掉了自证输出行与调用方式 | ❌ | 无 | **m10** |

---

## 四、给主会话的处置建议（按 ROI 排序）

1. **M2 与 M5 进项目记忆**——这两条是可跨任务复用的判据，不是本次事件的细节。M2 补进 `git-commit-takes-the-whole-index`（紧挨「别用按内容标记筛 hunk」），M5 补进 `the-pattern-is-not-the-predicate` 或独立成条。
2. **M1、M3、M4 补进对应文档**——M1 一行进收尾文档 §一条被放弃的路线；M3 一段进收尾文档；M4 的 rename 形态进记忆的配方。
3. **m6、m7、m10 各一行进索引事故文档**——都是给那个已成配方的代码块加范围与调用提示。
4. **m8 一行进收尾文档交付表**（补 `.dev` 侧提交）。
5. **m9 与 A1 只需在原句上补权重档**，不需要新证据。
6. **A2 请主会话核对**：若曾据「两份文档都是我写的」评估过记录密度，需要按剔除补记后的版本重估。

**流程层面的一条**：本轮 5 条 major 里有 3 条（M1、M3、M5）的时间距离都在 **15 分钟以内**——作者在 transcript 里明确说出了那句话，然后在几分钟后写的收尾文档里没有它。派发提示词里的先例（「作者最看不见自己刚做过的事」）在本轮再次成立，且形状更隐蔽：不是忘了那个坑，而是**只记了坑，没记出坑的那条路**。建议下一次收尾时，把「本会话我改过判据/换过做法的每一处，最终采用的是哪一版」单列一问。

---

## 五、评审自身的边界

- **派发提示词要求的 skill `my-skills:as-reviewer` 不存在**。我核过运行时 skill 列表与 `~/.claude/skills/` 目录，两处都没有该名字（相邻的是 `adopting-agent-findings`、`verifying-authoritative-claims`）。我改用了 `claude-code-transcripts`（解析 transcript）并按 `verifying-authoritative-claims` 的判据核实归属。**这不影响本次交付的完整性**，但请主会话核实那个 skill 名是否写错或已退役。
- 我只读了 transcript、上述四类承载物、以及为核实归属跑的若干只读 `git` 命令。**未修改任何代码或文档，未 commit / reset / stash，未触碰索引。**
- transcript 的 `thinking` 块在本文件中内容为空，所以**作者的内部推理不可读**。我的枚举建立在工具调用、工具输出与助手可见文本上；如果某条结论只在 thinking 里成形并被否决，我看不见它。
- 我没有独立验证作者那些标定值背后的测试是否正确——我只验证了数字在 transcript 里存在且一致（1211/1614/1617/1639/1643/1658/1660 逐条核过）。测试本身对不对不在本次范围。
- 本轮候选枚举是我一人一遍的产物。先例说「一轮不够」；按同样的理由，我这一轮也可能有漏。**类别 3（无失败信号的那一类）我做了两遍扫描**（一遍按时间顺序、一遍按关键词 `改用|放弃|不可信|误|其实|重新|作废|站不住|不成立|错了|推翻|中止` 反查），其余类别只做了一遍。

---

VERDICT: 0 blocker / 5 major / 7 minor

本轮独立枚举出的候选总数 42 / 其中作者未列 18 / 其中无耐久承载物 11（另 2 条为部分承载：#10、#28）
