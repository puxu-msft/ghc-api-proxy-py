# Current main 最终定向代码复核

- **评审范围**：`/home/xp/src/ghc-api-proxy-py` 的 current `main@e9fb2771d6e040c761bb4074e3fcf2547caece28`。只复核本轮 capability、History、Responses stream route、S3 graceful timeout、S4 rootless user installer 的合并态接口接缝，重点覆盖关键主路径、History lifecycle、stream cleanup、config／systemd installer 与无意回退。不执行完整 Acceptance／证明矩阵，不复核当前并行修改中的 living docs，不操作服务、manager、cgroup、安装或 cutover。
- **总体 verdict**：**修复 major 后可继续。** 当前未发现 blocker；发现 1 个 major：Responses stream 请求的 request conversion／degradation facts 未进入 `RequestContext` 与 History。该问题不阻断 stream wire 主路径，但违反 accepted Spec 的 History 合同，因此不满足“0 major 可继续”。
- **blocker 数**：0。
- **major 数**：1。
- **双视角覆盖证据——机械核对**：现场解析并固定物理仓库、`main` 分支与完整 HEAD；枚举 `bd86207… → 38bb06f… → ae84aa9… → c53849e… → e9fb277…` 的提交路径；对账五个 archive refs、reviewed／adapted source 与 main patch identity／结果 blob。Capability main patch 与 reviewed source一致；History main patch与 capability-parent适配提交 `b647991…`一致；stream main patch与 History-parent适配提交`b5d5d0c…`一致；S3 main patch与 reviewed source一致；S4三个结果 blob与适配 source `d3fabfa…`一致。逐文件读取最终 `client.py`、`executor.py`、`context.py`、`history/consumer.py`、stream parser／delivery／route／cleanup、config／CLI、system unit与installer。运行八个直接相关测试文件，执行结果与 collect-only交叉核对均为 `121` 项，执行全部通过；口径是本报告列明的八个路径、commit `e9fb277…`，不是全仓或完整证明矩阵。测试前后 `src/`、`tests/`、`contrib/` 相对 HEAD 的 tracked diff均为空。
- **双视角覆盖证据——第一人称执行**：模拟 Responses-only请求经 resolved-model capability选择、每 attempt `PRE_SEND` 后重转换、non-stream最终wire校验与成功／失败History回调；模拟stream从 `response.created`、完整block、terminal到 delayed headers／body，分别走正常完成、首body outcome uncertain、prefetch期间client disconnect、cleanup中再次cancel、postcommit protocol error、`max_output_tokens`成功终态；模拟S3配置默认／YAML／env／CLI优先级与Uvicorn graceful timeout，再以S4使用者身份走默认dry-run、`--check`、临时`--apply`、重复幂等与真实`systemd-analyze --user verify`。最后加入含`system[0].cache_control`与非allowlist metadata的真实stream请求，发现converter已产生facts而pipeline／History丢失。

## 事实性发现

[major] `src/app/pipeline/executor.py:327-382`、`src/app/history/consumer.py:145-165` — Responses stream 请求的 request conversion／degradation facts 被静默丢弃，History错误记录为空 — `AnthropicClient._send_responses()`在stream成功路径仍通过`AnthropicAttemptResult.converted_request_facts`返回转换事实（`src/app/anthropic/client.py:235-270`），但`execute_anthropic_pipeline()`只在`if not request.stream`分支把这些facts写入`context.conversion_facts`；stream转入`RequestState.STREAMING`时context仍为空。随后`HistoryConsumer._stream_usage_summary()`无条件写入`"conversion_facts": []`。只读定向探针使用同时包含`system[0].cache_control`和非allowlist`metadata.tenant`的Responses stream请求，输出为`PRODUCED_FACTS=2`、`PRODUCED_PATHS=system[0].cache_control,metadata.tenant`、`PIPELINE_CONTEXT_FACTS=0`。这违反accepted Spec：`docs/agents/anthropic-responses-bridge/spec.md:142`要求`DEGRADE`损失作为结构化`ConversionFact`进入History，`:421`要求History保留capability／degradation facts。现有stream smoke只断言response、usage、delivery与finalize，没有断言conversion facts，故八文件绿灯无法检出该回归。失败场景是任何Responses stream请求发生合法degrade时，客户端wire可成功但审计History声称无转换损失，stream／non-stream对同一语义请求产生不一致事实。— 修复建议：在每个attempt获得`AnthropicAttemptResult`后、stream与non-stream分支分叉之前，把该attempt的request conversion facts投影到`RequestContext`；stream terminal再合并stream侧response／usage facts，并让`HistoryConsumer._stream_usage_summary()`序列化context中的最终成功attempt facts，而不是固定空列表。新增真实`HistoryConsumer`的stream回归测试，至少覆盖成功terminal、postcommit failure／partial History和retry后只保留最终成功attempt；同时保留失败路径零成功facts与exactly-once finalize断言。

## 结论

除上述major外，本次限定范围内未发现其他blocker或major。Capability fail-closed与每attempt重转换、non-stream History事实与回调顺序、stream主路径与cleanup、S3 graceful timeout及S4 installer组合合同在本轮静态走查和最小相关测试中未见无意回退。完整retry、quota／resident backpressure、真实socket partial-write／RST、完整History矩阵、真实user-manager／effective cgroup与完整Acceptance仍不在本报告证明范围内。
